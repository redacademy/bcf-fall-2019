import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  signInBgImg: {
    position: 'absolute',
    height: '100%',
    width: '100%',
  },
  title: {
    marginTop: 116,
    textAlign: 'center',
  },
});

export default styles;
